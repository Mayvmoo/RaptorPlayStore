<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Raptor" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryDark">@color/dark_blue</item>
        <item name="colorAccent">@color/gold</item>
    </style>

    <style name="Theme.Raptor" parent="Base.Theme.Raptor" />
    
    <!-- DatePicker Theme -->
    <style name="DatePickerTheme" parent="Theme.Material3.DayNight.Dialog">
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorAccent">@color/gold</item>
        <item name="android:textColorPrimary">@color/black</item>
    </style>
    
    <!-- TimePicker Theme -->
    <style name="TimePickerTheme" parent="Theme.Material3.DayNight.Dialog">
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorAccent">@color/gold</item>
        <item name="android:textColorPrimary">@color/black</item>
    </style>
</resources>